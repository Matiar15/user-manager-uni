spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: root
    password: root
    driver-class-name: org.postgresql.Driver

  flyway:
    create-schemas: true
    schemas: user_manager_uni
    default-schema: user_manager_uni

jwt:
  key: TEST_TEST_TEST_TEST_TEST_TEST_TEST_TEST
  expires-after: 3600
  refresh-after: 3600
