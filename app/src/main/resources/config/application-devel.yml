spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: root
    password: root
    driver-class-name: org.postgresql.Driver

  flyway:
    create-schemas: true
    schemas: user_manager_uni
    default-schema: user_manager_uni
